# Production build (mode=production). Committed; non-secret.
# Same-origin serverless function (api/updates.js) — no CORS needed. Requires
# POSTGRES_URL on the Vercel project (server-side only; never a VITE_ var).
VITE_UPDATES_API_URL=/api/updates
VITE_UPDATES_IMAGE_BASE=
