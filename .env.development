# Local dev (mode=development). Committed; non-secret.
# The devlog feed comes from the local CORS shim: run `npm run updates:proxy`
# in a second terminal and it relays the live deploy. Leave empty to fall back
# to the bundled seed entries (src/data/updates.ts).
VITE_UPDATES_API_URL=http://localhost:8787/updates
VITE_UPDATES_IMAGE_BASE=
